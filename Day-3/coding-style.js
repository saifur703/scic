// Reference
// https://javascript.info/coding-style

/* 
Code must be as clean and easy to read as possible. To ensure this we need to follow some rules.

*/
// 1. Curly brace
function add(a, b) {
  // code
}

// 2. Space around operator
let n = 12;
if (n < 24) {
  console.log('Something');
}

// 3. Semicolon: though using semicolon is not necessary in JavaScript, we should use it
let myName = 'Saifur Rahman';

// 4. A Space between parameters
function add(a, b) {
  //some code
  return a, b;
}
