/**
 * DOM node property is just javascript object
 * It is prototype based class for inheritance
 *
 */

/**
 * Main DOM node properties are:
 * bodetype,
 * nodeName,
 * tagName,
 * innerHTML,
 * outerHTML,
 * nodeValue/dnta,
 * textContent,
 * hidden
 */

document.body.nodeName; // BODY
document.body.tagName; // BODY

/**
 * innerHTML: It gives the HTML content of the element.
 */
document.body.outerHTML;
